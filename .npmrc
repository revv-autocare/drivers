# Hoist all deps to the root node_modules so pnpm exec finds binaries
# from any workspace dir. Amplify Gen 2 requires this for CDK asset
# bundling (it calls `pnpm exec esbuild` from the repo root, but esbuild
# is a transitive dep of vite living under apps/driver).
public-hoist-pattern[]=*
